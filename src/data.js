export default[
    {
        value:Math.ceil(Math.random()*6),
        id:1,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:2,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:3,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:4,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:5,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:6,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:7,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:8,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:9,
        isHeld:false
    },

    {
        value:Math.ceil(Math.random()*6),
        id:10,
        isHeld:false
    },










]